# =====================================================================
# Dependências de runtime — Monitoramento Multimodal de Pacientes
# Python 3.10+
# =====================================================================

# --- Núcleo / dados ---
numpy>=1.24,<2.0
pandas>=2.0
scipy>=1.10

# --- Detecção de anomalias (RF03) ---
scikit-learn>=1.3

# --- Configuração / validação ---
pydantic>=2.5
pydantic-settings>=2.1
python-dotenv>=1.0

# --- API / orquestração ---
fastapi>=0.110
uvicorn[standard]>=0.27
python-multipart>=0.0.9

# --- HTTP (webhooks de alerta) ---
httpx>=0.27

# --- CLI / UX ---
rich>=13.7

# =====================================================================
# Dependências OPCIONAIS (pesadas / com hardware ou nuvem).
# A aplicação degrada graciosamente para modo "mock" quando ausentes,
# permitindo demonstração local. Instale conforme necessidade:
#
#   Visão computacional (RF01):
#     pip install opencv-python mediapipe ultralytics
#
#   Áudio / Azure (RF02):
#     pip install azure-cognitiveservices-speech azure-ai-textanalytics
# =====================================================================
